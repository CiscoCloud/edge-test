# {{ ansible_managed }}

# options supported by mesos-init-wrapper
IP={{ ansible_default_ipv4.address }}
CLUSTER="{{ cluster }}"
LOGS="{{ log_dir }}"
ULIMIT="{{ ulimit }}"

# environment variables
#export MESOS_HOSTNAME={{ ansible_hostname }}.node.{{ consul_dc }}
export MESOS_HOSTNAME={{ inventor_hostname }}
