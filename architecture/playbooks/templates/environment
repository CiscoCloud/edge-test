NAME=zookeeper
ZOOCFGDIR=/opt/zookeeper-{{ zookeeper_version }}/conf
ZOOLIBDIR=/opt/zookeeper-{{ zookeeper_version }}/lib

CLASSPATH="$ZOOCFGDIR:$ZOOLIBDIR"

ZOOCFG="$ZOOCFGDIR/zoo.cfg"
ZOO_LOG_DIR=/var/log/$NAME
USER=root
GROUP=root
# TODO
#USER=$NAME
#GROUP=$NAME
PIDDIR=/var/run/$NAME
PIDFILE=$PIDDIR/$NAME.pid
SCRIPTNAME=/etc/init.d/$NAME
JAVA=/usr/bin/java
ZOOMAIN="org.apache.zookeeper.server.quorum.QuorumPeerMain"
ZOO_LOG4J_PROP="INFO,ROLLINGFILE"
JMXLOCALONLY=false
JAVA_OPTS=""